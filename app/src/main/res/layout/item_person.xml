<?xml version="1.0" encoding="utf-8"?>
<!--
    One person in the horizontal people strip on the home screen.

    Fixed width so the strip scrolls in even steps rather than jittering when
    a long name follows a short one; the name itself is capped at one line and
    ellipsised for the same reason.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="78dp"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingVertical="6dp">

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/personAvatar"
            android:layout_width="58dp"
            android:layout_height="58dp"
            android:contentDescription="@null"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_profile"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.ChatCircle.Avatar"
            app:strokeColor="@color/konecta_outline"
            app:strokeWidth="1dp" />

        <!--
            Presence dot, parked on the avatar edge. Its white ring keeps it
            legible when it lands on a dark part of a photo.
        -->
        <View
            android:id="@+id/personPresence"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="bottom|end"
            android:background="@drawable/bg_presence_online" />

    </FrameLayout>

    <TextView
        android:id="@+id/personName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:ellipsize="end"
        android:fontFamily="@font/poppinsmedium"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/konecta_ink"
        android:textSize="12sp"
        tools:text="Lojain" />

</LinearLayout>
